{% extends 'base.html.twig' %}

{% block add %}
    {% if user %}
        <h5>Congratulations</h5>
        <p>Your account is confirmed! You can now sign in
            <a href="{{ path('login') }}">here</a></p>
    {% else %}
        Something went wrong. Please check your e-mail
    {% endif %}
{% endblock %}
